---
description: Create a new React component with myOperator design system
argument-hint: Optional screenshot path
allowed-tools: ["Read", "Write", "Edit", "Bash", "Glob", "Grep", "AskUserQuestion", "Task", "Skill"]
---

# Create Component

Use the `component-analysis` and `myoperator-design` skills to create a new component following the myOperator design system.

1. Analyze requirements using `component-analysis` skill
2. Design using `myoperator-design` skill  
3. Validate with `design-system-validator` skill
4. Generate stories with `storybook-generator` skill
